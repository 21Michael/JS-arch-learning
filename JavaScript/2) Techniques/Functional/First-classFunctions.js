/*
First class function / object -  is a function that does the following:
  - can be created via literals;
  - assign to variable, object property, array value;
  - passed as an argument to a function;
  - returned as value from function;
  - posses own properties (static);
 */